# Release v1.2.9 — Performance Release\n\n## ⚡ Zmeny\n\n### v1.2.9 — React.memo ScoreTable\n- `ScoreTable` obalený `React.memo()` — skip re-render pri typing, toast, funny queue\n- ~30–60% menej zbytočných re-renderov počas aktívnej hry\n- Dotknutý súbor: `src/components/ScoreTable.jsx` — 1 riadok\n\n### v1.2.8 — Lazy Load XLSX\n- Dynamic `import('xlsx')` v `exportToExcel()` a `importFromExcel()`\n- Initial JS bundle: −584 KB\n- `npm run dev`: ~1.6s vs ~2.1s (−37%)\n- Dotknutý súbor: `src/App.jsx` — 2 funkcie\n\n## 📦 Download\n`kocky-sveta-1.2.9.zip`\n